package I.Good;
/**
 *  Food for thought: better name should be "Readable" instead of "Reader".
 * Komentras
 */
public interface Reader {
    /**
     * Load
     * @param path kelias i faila
     */
    void read(String path);
}
